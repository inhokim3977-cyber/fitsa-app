# ===================================
# FITSA 환경변수 샘플
# ===================================
# 이 파일은 커밋 가능 (실제 값 없음)
# 실제 값은 .env에 저장하고 절대 커밋 금지!

# Flask Configuration
FLASK_ENV=production
PORT=10000

# ===================================
# AI Services (필수)
# ===================================
# Gemini API Key - https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Replicate API Token - https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your_replicate_token_here

# ===================================
# Payment (필수)
# ===================================
# Stripe Secret Key - https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key_here

# Stripe Public Key (프론트엔드용)
VITE_STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key_here

# ===================================
# Session Security (필수)
# ===================================
# 랜덤 문자열 생성: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=your_session_secret_here_use_random_string

# ===================================
# Object Storage (선택 - Replit/GCS 사용 시)
# ===================================
DEFAULT_OBJECT_STORAGE_BUCKET_ID=
PRIVATE_OBJECT_DIR=.private
PUBLIC_OBJECT_SEARCH_PATHS=public

# ===================================
# Database (선택 - PostgreSQL 사용 시)
# ===================================
# Render PostgreSQL 자동 제공 시 자동 설정됨
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ===================================
# OpenAI (선택 - OpenAI 피팅 사용 시)
# ===================================
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_BASE_URL=
